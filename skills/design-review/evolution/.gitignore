# Per-environment evaluator snapshots — warning counts can vary by Playwright
# version, so they are regenerated locally (regression-gate.mjs --baseline),
# not committed. The ledger + rule registry ARE committed: they are the
# accumulated history/knowledge the self-evolution loop builds on.
regression-baseline.*.json
