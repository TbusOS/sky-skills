{
  "id": "KV-063",
  "topic": "IIO private data via iio_priv",
  "q": "IIO 驱动的私有状态(regmap、配置等)放哪?要不要单独 kmalloc?",
  "kind": "objective",
  "canary": true,
  "subsystem": "iio",
  "gold_claims": {
    "api": ["devm_iio_device_alloc", "iio_priv"],
    "config": ["CONFIG_IIO"]
  },
  "corruptions": [
    { "type": "fake-api", "claim": "iio_read_channel_value", "why": "编造的读值函数" }
  ],
  "forbidden": ["私有数据单独 kmalloc 而不用 iio_priv"],
  "rubric": "私有数据跟 iio_dev 一起分配:devm_iio_device_alloc(dev, sizeof(state)),用 iio_priv(indio_dev) 取,不要另外 kmalloc;CONFIG_IIO 是框架开关"
}
